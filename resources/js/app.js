require('./bootstrap');


// export module for unknown guests
  // on button approve 
      // get the information from the row
      // great object and pass through to api
  // on button deny
      // call reject api, pass through the guest ID
